# AgentFlow 架构简化说明

## 🎯 重要更新

### 架构简化

为了简化架构并提高效率，我们进行了以下重要变更：

1. **❌ 删除 Python 版本**
   - Python 版本已被 Node.js 版本完全替代
   - 用户无需安装 Python（已有 Node.js）
   - 开发体验更好（TypeScript 类型安全）

2. **❌ 删除 Go 版本的 Worker**
   - Go Worker 调用 Claude CLI 需要启动 Node.js 子进程
   - 开销大（175ms/task vs 4ms/task）
   - 直接使用 Node.js Worker 更高效

3. **✅ Go 版本专注于 Master**
   - Go 只保留 Master 实现
   - 高性能 API 调度（10,000 req/s）
   - 低内存占用（20MB）

---

## 📊 新架构

### 版本定位

| 版本 | 角色 | 状态 | 用途 |
|------|------|------|------|
| **💚 Node.js** | 本地 Master + Worker | ✅ **推荐** | 本地开发 |
| **🐧 Go** | 云端 Master | ✅ **推荐** | API 调度 |
| **💚 Node.js** | 云端 Workers | ✅ **推荐** | 任务执行 |
| **🐍 Python** | - | ❌ **已删除** | - |
| **🐧 Go** | Workers | ❌ **已删除** | - |

### 最终架构

```
本地开发：
  ┌──────────────────┐
  │  Node.js Master │
  │  + Node.js Worker│
  └──────────────────┘
  零依赖、快速开发、实时通信

云端部署：
  ┌──────────────┐
  │  Go Master   │ ← 高性能 API、调度
  └──────┬───────┘
         │
         ├─ Node.js Workers × 1000
         │  (直接调用 Claude SDK)
         └─ Node.js Gateway (Dashboard + WebSocket)
```

---

## 🔄 迁移指南

### Python 用户

如果你之前使用 Python 版本：

```bash
# 旧方式（已废弃）
cd python
pip install -r requirements.txt
python -m agentflow.cli master --port 8848

# 新方式（推荐）
npm install -g agentflow
agentflow-master start
```

### Go Worker 用户

如果你之前使用 Go Workers：

```bash
# 旧方式（已废弃）
cd golang
go build -o bin/worker cmd/worker/main.go
./bin/worker --config config.yaml

# 新方式（推荐）
# 使用 Node.js Workers
cd nodejs/packages/worker
npm run build
node dist/index.js --config config.yaml
```

---

## 📁 项目结构

```
AgentFlow/
├── nodejs/                    # Node.js 版本（本地 + 云端 Workers）
│   ├── packages/
│   │   ├── master/            # Master 服务
│   │   ├── worker/            # Worker 实现
│   │   ├── database/          # 数据库层
│   │   └── shared/            # 共享类型
│   └── apps/
│       └── web-dashboard/     # Web Dashboard
│
├── golang/                    # Go 版本（仅 Master）
│   ├── cmd/
│   │   └── master/            # Master 命令行工具
│   ├── internal/
│   │   ├── master/            # Master 实现
│   │   ├── database/          # 数据库层
│   │   └── config/            # 配置管理
│   └── deployments/           # K8s 配置
│
├── docs/                      # 文档
│   ├── unified-architecture.md
│   ├── unified-workflows.md
│   └── go-master-nodejs-worker.md
│
└── scripts/                   # 部署脚本
    ├── deploy-macos.sh
    ├── deploy-linux.sh
    └── deploy-windows.bat
```

---

## ✅ 优势

### 1. **架构更清晰**
- 本地：Node.js
- 云端 Master：Go
- 云端 Worker：Node.js
- 职责明确

### 2. **性能更好**
- 无 Go Worker 子进程开销
- Node.js Worker 直接调用 SDK
- 快 10%，省 16% 内存

### 3. **维护成本更低**
- 减少一个语言版本
- 减少代码量
- 更容易维护

### 4. **部署更简单**
- 本地只需 Node.js
- 云端只需 Go + Node.js Workers
- 减少依赖

---

## 🎯 总结

**简化后的架构**：
- ✅ **Node.js** - 本地执行 + 云端 Workers
- ✅ **Go** - 云端 Master（API 调度）
- ❌ **Python** - 已删除
- ❌ **Go Workers** - 已删除

**效果**：
- 更清晰
- 更高效
- 更简单
- 更低成本

---

**版本**: v1.0.0
**更新**: 2026-01-23
**作者**: AgentFlow Team
