{
  "name": "@agentflow/local-executor",
  "version": "1.0.0",
  "description": "Local task execution manager for AgentFlow",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "agentflow",
    "task-execution",
    "local",
    "automation"
  ],
  "author": "AgentFlow Team",
  "license": "MIT",
  "dependencies": {
    "@agentflow/shared": "workspace:*",
    "@agentflow/worker": "workspace:*",
    "@agentflow/skill": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
