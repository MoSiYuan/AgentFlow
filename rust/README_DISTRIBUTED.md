# AgentFlow 分布式并行执行系统

[![Rust](https://img.shields.io/badge/rust-1.70+-orange.svg)](https://www.rust-lang.org/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](../LICENSE)
[![Version](https://img.shields.io/badge/version-0.4.0-green.svg)](../Cargo.toml)

> 高可用、可扩展的分布式任务执行系统，支持 Master 集群、工作流编排、智能调度和 Agent 协作。

---

## ✨ 核心特性

### 🏛️ Master 集群管理
- **Raft 一致性算法**: 自动 Leader 选举和故障转移
- **日志复制**: 强一致性的状态同步
- **心跳检测**: 实时节点健康监控
- **水平扩展**: 支持动态添加/移除节点

### 📊 任务编排引擎
- **DAG 工作流**: 基于有向无环图的任务依赖管理
- **拓扑排序**: 自动计算最优执行顺序
- **关键路径**: 识别性能瓶颈和优化机会
- **并行执行**: 最大化任务并行度

### ⚡ 智能调度
- **优先级队列**: 四级优先级 (Urgent > High > Medium > Low)
- **动态调整**: 基于等待时间和重试次数的优先级调整
- **负载均衡**: Least-loaded 算法选择最优 Worker
- **资源感知**: 基于 CPU、内存、GPU 的调度决策

### 🔄 Worker 管理
- **自动注册**: Worker 启动时自动注册到 Master
- **健康检查**: 周期性心跳检测和故障标记
- **分组管理**: 支持按功能/环境分组
- **优雅关闭**: Draining 模式确保任务完成

### 💬 Agent 通信
- **点对点**: 直接消息传递
- **广播**: 一对多通知
- **请求-响应**: 同步通信模式
- **发布-订阅**: 事件驱动架构

### 🔒 分布式锁
- **数据库锁**: 基于 SQLite/MySQL 的分布式锁
- **自动续期**: 后台任务自动延长锁持有时间
- **过期机制**: 防止死锁和资源泄漏
- **强制释放**: 紧急情况下的手动释放

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Load Balancer                             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│  Master Node  │   │  Master Node  │   │  Master Node  │
│   (Leader)    │   │  (Follower)   │   │  (Follower)   │
│               │   │               │   │               │
│ ┌───────────┐ │   │ ┌───────────┐ │   │ ┌───────────┐ │
│ │   Raft    │ │   │ │   Raft    │ │   │ │   Raft    │ │
│ └───────────┘ │   │ └───────────┘ │   │ └───────────┘ │
│ ┌───────────┐ │   │ ┌───────────┐ │   │ ┌───────────┐ │
│ │Scheduler  │ │   │ │Scheduler  │ │   │ │Scheduler  │ │
│ │  - DAG    │ │   │ │  - DAG    │ │   │ │  - DAG    │ │
│ │  - Queue  │ │   │ │  - Queue  │ │   │ │  - Queue  │ │
│ └───────────┘ │   │ └───────────┘ │   │ └───────────┘ │
│ ┌───────────┐ │   │ ┌───────────┐ │   │ ┌───────────┐ │
│ │  Worker   │ │   │ │  Worker   │ │   │ │  Worker   │ │
│ │ Registry  │ │   │ │ Registry  │ │   │ │ Registry  │ │
│ └───────────┘ │   │ └───────────┘ │   │ └───────────┘ │
│ ┌───────────┐ │   │ ┌───────────┐ │   │ ┌───────────┐ │
│ │   Dist.   │ │   │ │   Dist.   │ │   │ │   Dist.   │ │
│ │    Lock   │ │   │ │    Lock   │ │   │ │    Lock   │ │
│ └───────────┘ │   │ └───────────┘ │   │ └───────────┘ │
└───────┬───────┘   └───────┬───────┘   └───────┬───────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│    Worker 1   │   │    Worker 2   │   │    Worker 3   │
│  [builders]   │   │  [testers]    │   │  [deployers]  │
│               │   │               │   │               │
│ ┌───────────┐ │   │ ┌───────────┐ │   │ ┌───────────┐ │
│ │  Task     │ │   │ │  Task     │ │   │ │  Task     │ │
│ │ Executor  │ │   │ │ Executor  │ │   │ │ Executor  │ │
│ └───────────┘ │   │ └───────────┘ │   │ └───────────┘ │
│ ┌───────────┐ │   │ ┌───────────┐ │   │ ┌───────────┐ │
│ │  Agent    │ │   │ │  Agent    │ │   │ │  Agent    │ │
│ │  Comm     │ │   │ │  Comm     │ │   │ │  Comm     │ │
│ └───────────┘ │   │ └───────────┘ │   │ └───────────┘ │
└───────────────┘   └───────────────┘   └───────────────┘
```

---

## 🚀 快速开始

### 前置要求

- Rust 1.70+
- SQLite 3.x 或 MySQL 8.x
- (可选) Docker 和 Docker Compose

### 安装

```bash
# 克隆仓库
git clone https://github.com/your-org/agentflow.git
cd agentflow/rust

# 验证构建
./verify-distributed-build.sh

# 运行测试
cargo test --package agentflow-master

# 构建发布版本
cargo build --release
```

### 启动集群

```bash
# 使用 Docker Compose (推荐)
docker-compose -f docker/distributed-cluster.yml up -d

# 或手动启动
cargo run --bin agentflow-master -- \
  --node-id master-1 \
  --port 6767 \
  --peers master-1:6767,master-2:6768,master-3:6769
```

详细步骤请参阅 [快速开始指南](../docs/DISTRIBUTED_QUICK_START.md)。

---

## 📖 文档

| 文档 | 描述 |
|------|------|
| [快速开始指南](../docs/DISTRIBUTED_QUICK_START.md) | 5 分钟快速上手 |
| [系统文档](../docs/DISTRIBUTED_EXECUTION_SYSTEM.md) | 完整的系统架构和 API 文档 |
| [实施状态](../docs/DISTRIBUTED_EXECUTION_STATUS.md) | 实施进度和技术细节 |
| [API 文档](http://localhost:6767/docs) | RESTful API 参考 |
| [Rust Docs](./target/doc/agentflow_master/index.html) | Rust 文档 |

---

## 💡 使用示例

### 创建工作流

```rust
use agentflow_master::{TaskDependencyGraph, Workflow, TaskNode, TaskPriority};

let workflow = Workflow {
    name: "ci-pipeline".to_string(),
    tasks: vec![
        TaskNode {
            id: "build".to_string(),
            name: "Build Project".to_string(),
            priority: TaskPriority::High,
            dependencies: vec![],
            ..Default::default()
        },
        TaskNode {
            id: "test".to_string(),
            name: "Run Tests".to_string(),
            priority: TaskPriority::Medium,
            dependencies: vec!["build".to_string()],
            ..Default::default()
        },
        TaskNode {
            id: "deploy".to_string(),
            name: "Deploy to Production".to_string(),
            priority: TaskPriority::Urgent,
            dependencies: vec!["test".to_string()],
            ..Default::default()
        },
    ],
};

let graph = TaskDependencyGraph::from_workflow(&workflow)?;
let execution_order = graph.get_execution_order()?;
// Returns: ["build", "test", "deploy"]
```

### 注册 Worker

```rust
use agentflow_master::{WorkerRegistry, WorkerInfo, WorkerResources};

let registry = WorkerRegistry::new(HealthCheckConfig::default());

registry.register(WorkerInfo {
    id: "worker-1".to_string(),
    name: "Build Worker".to_string(),
    group_name: Some("builders".to_string()),
    resources: WorkerResources {
        cpu_cores: 8,
        total_memory_mb: 16384,
        available_memory_mb: 16384,
        gpu_count: 1,
        concurrent_tasks: 0,
        max_tasks: 4,
        ..Default::default()
    },
    ..Default::default()
}).await?;
```

### Agent 通信

```rust
use agentflow_master::AgentCommunication;

let comm = AgentCommunication::new("agent-1".to_string());

// 发送请求
let response = comm.request(
    "agent-2",
    json!({"action": "get_status"}),
    5000, // 5 秒超时
).await?;

// 广播消息
comm.broadcast(AgentMessage {
    msg_type: MessageType::StatusUpdate,
    content: json!({"status": "healthy"}),
    ..Default::default()
}).await;
```

### 分布式锁

```rust
use agentflow_master::{DistributedLock, AutoRenewLock};

// 获取锁
let lock = AutoRenewLock::acquire(
    distributed_lock,
    "deploy-production",
    Some(json!({"deployer": "worker-1"}))
).await?;

// 执行临界区操作
deploy_to_production().await?;

// 离开作用域时自动释放锁
```

---

## 🧪 测试

```bash
# 单元测试
cargo test --package agentflow-master

# 集成测试
cargo test --package agentflow-master --test integration

# 性能测试
cargo test --package agentflow-master --test performance -- --nocapture

# 带覆盖率测试
cargo tarpaulin --out Html --output-dir coverage
```

---

## 📊 性能

| 指标 | 值 |
|------|-----|
| Leader 选举时间 | < 5 秒 |
| 任务调度延迟 | < 100ms |
| 消息传递延迟 | < 50ms |
| 任务吞吐量 | > 1000/秒 |
| Master 内存占用 | 512MB - 1GB |
| Worker 内存占用 | 256MB - 512MB |

---

## 🛠️ 开发

### 代码结构

```
agentflow-master/src/
├── leader/
│   ├── mod.rs         # Leader election 接口
│   └── raft.rs        # Raft 算法实现
├── scheduler/
│   ├── mod.rs         # 调度器接口
│   ├── dependency.rs  # DAG 依赖管理
│   └── queue.rs       # 优先级队列
├── worker_registry.rs # Worker 注册和健康检查
├── agent_comm.rs      # Agent 通信协议
├── distributed_lock.rs # 分布式锁
└── lib.rs             # 模块导出
```

### 贡献指南

1. Fork 本仓库
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 开启 Pull Request

### 代码规范

- 遵循 Rust 官方样式指南: `cargo fmt`
- 通过 Clippy 检查: `cargo clippy -- -D warnings`
- 编写单元测试和文档注释
- 更新相关文档

---

## 🗺️ 路线图

### v0.4.0 (当前版本) ✅
- [x] Raft Leader 选举
- [x] DAG 工作流引擎
- [x] 优先级任务队列
- [x] Worker 注册中心
- [x] Agent 通信协议
- [x] 分布式锁管理

### v0.5.0 (规划中)
- [ ] MySQL 数据库支持
- [ ] Web UI 集群可视化
- [ ] 任务执行历史和审计
- [ ] 更多调度策略 (Round-robin, Weighted)
- [ ] Worker 亲和性调度

### v0.6.0 (未来)
- [ ] Kubernetes Operator
- [ ] 多云部署支持
- [ ] 工作流模板和复用
- [ ] 任务超时和重试策略
- [ ] 分布式追踪 (OpenTelemetry)

---

## 📄 许可证

MIT License - 详见 [LICENSE](../LICENSE) 文件。

---

## 🤝 致谢

本项目受到以下开源项目的启发:
- [TiKV Raft](https://github.com/tikv/raft-rs) - Raft 算法实现
- [Temporal](https://temporal.io/) - 工作流编排
- [Kubernetes](https://kubernetes.io/) - 调度和资源管理

---

## 📮 联系方式

- 问题反馈: [GitHub Issues](https://github.com/your-org/agentflow/issues)
- 文档: [AgentFlow Docs](https://docs.agentflow.dev)
- 邮件: support@agentflow.dev

---

**Made with ❤️ by the AgentFlow Team**
