# AgentFlow 项目配置

## 1. 构建系统

### Windows
- 使用 `msbuild` 进行编译
- 示例：`msbuild Project.sln /p:Configuration=Release`

### Linux/macOS
- 使用 `make` 或 `cargo build`
- 示例：`cargo build --release`

## 2. 测试工作流

### 正确的测试命令
```bash
# 不要运行 `make test`
# 正确命令：
./bin/test --suite=unit
cargo test --lib
```

### 跳过的测试
- 某些集成测试需要特定环境，使用 `--skip` 标记

## 3. 关键技能 (Do's and Don'ts)

### ✅ DO (应该做)
- 提交代码前必须运行 linter：`cargo clippy -- -D warnings`
- 修改 API 后更新文档
- 遵循 Rust 命名规范

### ❌ DON'T (不应该做)
- 不要修改 `third_party/` 目录下的文件
- 不要硬编码路径，使用相对路径
- 不要提交 `target/` 目录

### 🔧 Special Skills
- Shader 报错时，删除 `Cache/` 目录
- 数据库迁移失败时，删除 `*.db-shm` 和 `*.db-wal` 文件

## 4. 调试策略

### 崩溃问题
- 优先检查 `Logs/` 目录
- 使用 `RUST_BACKTRACE=1` 获取详细堆栈

### 性能问题
- 使用 `cargo flamegraph` 分析热点
- 检查是否有不必要的克隆

## 5. 项目特定信息

### 架构说明
- [描述项目的整体架构]

### 依赖关系
- [列出关键依赖和它们的用途]

### 常见问题
- [记录常见问题和解决方案]
